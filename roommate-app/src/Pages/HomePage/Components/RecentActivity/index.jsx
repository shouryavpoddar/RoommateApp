import { Text, View } from 'react-native';

export default function RecentActivity() {
    return (
        //Idea is to pull most recent 3 activities from the Redux recent activity items and display them on widget
        //Clicking the widget could give popup or page to show scrollable list of most recent activity items...
        <View className="w-full overflow-hidden rounded-md">
        </View>
    )
}